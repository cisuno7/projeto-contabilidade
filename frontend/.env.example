# URL base da API (backend)
# Em desenvolvimento: use /api (proxy do Vite redireciona para localhost:8080)
# Em produção: use a URL completa do backend, incluindo o prefixo /api
VITE_API_URL=https://projeto-contabilidade.onrender.com/api
